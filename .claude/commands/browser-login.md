動作確認用のテストユーザーでブラウザにログインしてください。

## テストユーザー情報

| 項目 | 値 |
|------|-----|
| メールアドレス | claudetest@example.com |
| パスワード | Test1234! |
| ユーザー名 | claudetest |

## 手順

1. Chrome DevTools MCP で `http://localhost:5173/login` にアクセスする
2. メールアドレス欄に `claudetest@example.com` を入力する
3. パスワード欄に `Test1234!` を入力する
4. ログインボタンをクリックする
5. ホーム画面（`/home`）にリダイレクトされたことを確認する

## 注意事項

- サーバーが起動していない場合は先に `/server-start` を実行すること
- すでにログイン済みの場合はそのまま動作確認を進めること
